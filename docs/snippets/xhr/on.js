// make a request - returns a XMLHttpRequest
xhr.request(props)

// listen to events - similar to setting parameters in the props object above
/***
	@[event]: any XHR event type, e.g. 'complete', 'error', 'progress' or 'abort'
*/
.on( event , callback)